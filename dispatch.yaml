dispatch:
  # Default module serves the typical web resources and all static resources.
  - url: "*/favicon.ico"
    module: default

  # Default module serves simple hostname request.
  - url: "portal.bonlineapp.com/"
    module: default

  # Send all mobile traffic to the service module.
  - url: "service.portal.bonlineapp.com/"
    module: service
